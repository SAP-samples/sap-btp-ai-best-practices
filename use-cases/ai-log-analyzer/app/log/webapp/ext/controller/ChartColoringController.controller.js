sap.ui.define(['sap/ui/core/mvc/ControllerExtension'], function (ControllerExtension) {
	'use strict';

	return ControllerExtension.extend('sap.btp.ai.log.ext.controller.ChartColoringController', {
		// this section allows to extend lifecycle hooks or hooks provided by Fiori elements
		override: {
			/**
             * Called when a controller is instantiated and its View controls (if available) are already created.
             * Can be used to modify the View before it is displayed, to bind event handlers and do other one-time initialization.
             * @memberOf sap.btp.ai.log.ext.controller.ChartColoringController
             */
			onInit: function () {
				// you can access the Fiori elements extensionAPI via this.base.getExtensionAPI
				var oModel = this.base.getExtensionAPI().getModel();
			},

			onAfterRendering: function () {
				// ID autogenerated by Fiori tools → check it in the UI5 inspector
				const oSmartChart = this.base.byId("sap.btp.ai.log::MessageTimeAggregatesList--fe::Chart::MessageTimeAggregates::alpChart::Chart");
				if (!oSmartChart) { return; }
				console.log(oSmartChart)
				// oSmartChart.attachInitialise(() => {
				//   oSmartChart.getChart().setVizProperties({
				// 	plotArea: {
				// 	  /* 1: low , 2: medium , 3: high , 4: no-value */
				// 	  colorPalette: [
				// 		"#107E3E", // Positive (green)
				// 		"#F1C232", // Critical  (orange)
				// 		"#D04F4F", // Negative  (red)
				// 		"#89919A"  // Neutral   (grey-blue)
				// 	  ]
				// 	}
				//   });
				// });
			  }
		}
	});
});
