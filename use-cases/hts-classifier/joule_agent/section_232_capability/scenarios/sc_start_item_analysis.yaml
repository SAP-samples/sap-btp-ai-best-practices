description: >
  Start a backend Section 232 prediction job for a single GCC tracker item.
  Use this when the user asks to analyze, classify, rerun, or start analysis
  for one item id. Examples: "Start analysis for gcc:1001", "classify item
  gcc:1001", or "rerun Section 232 analysis for gcc:2002 with PDFs".

slots:
  - name: item_id
    description: >
      Backend GCC tracker item id to classify, in the form gcc:1001. Capture
      the complete value including the prefix and colon.
  - name: document_mode
    description: >
      Evidence mode for the prediction. Default to text_only. Use
      with_documents only when the user explicitly asks to include PDFs,
      diagrams, drawings, or uploaded documents.

target:
  name: fn_start_item_analysis
  type: function
