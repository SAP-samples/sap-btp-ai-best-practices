applications:
  - name: postsale-chatbot
    memory: 256M
    instances: 1
    path: .
    buildpacks:
      - python_buildpack
    command: gunicorn main_ui:app -b 0.0.0.0:$PORT # Bind to host 0.0.0.0 and CF-provided PORT
    env:
      FLASK_ENV: production
      PYTHONUNBUFFERED: 'True'
